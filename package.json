{
  "name": "sgualda-com",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/build-og.mjs && node scripts/build-sitemap-dates.mjs && node scripts/build-htaccess.mjs",
    "preview": "astro preview",
    "check": "astro check",
    "audit:urls": "node scripts/check-urls.mjs",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "cms": "CMS=1 astro dev",
    "audit:budget": "node scripts/check-budget.mjs",
    "audit:quality": "node scripts/sync-quality.mjs",
    "audit:prose": "node scripts/check-prose.mjs",
    "setup:nosync": "for d in dist .astro test-results; do [ -L $d ] || { rm -rf $d; mkdir -p $d.nosync; ln -sfn $d.nosync $d; }; done && echo '  ✓ dist, .astro and test-results kept out of iCloud'",
    "audit": "npm run audit:urls && npm run audit:budget && npm run audit:quality && npm run audit:prose"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.12",
    "@astrojs/sitemap": "^3.6.0",
    "astro": "^5.14.1",
    "sharp": "^0.34.4"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "@astrojs/react": "^6.0.2",
    "@axe-core/playwright": "^4.12.1",
    "@keystatic/astro": "^5.2.0",
    "@keystatic/core": "^0.6.4",
    "@playwright/test": "^1.62.1",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.4",
    "http-server": "^14.1.1",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "typescript": "^5.9.3"
  }
}
